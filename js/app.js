const root=document.documentElement,themeButton=document.querySelector('.theme-toggle'),menuButton=document.querySelector('.menu-toggle'),links=document.querySelector('.links');
const savedTheme=localStorage.getItem('sayohat-theme');if(savedTheme)root.dataset.theme=savedTheme;function updateThemeIcon(){if(themeButton)themeButton.textContent=root.dataset.theme==='dark'?'☀':'☾'}updateThemeIcon();
themeButton?.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('sayohat-theme',root.dataset.theme);updateThemeIcon()});menuButton?.addEventListener('click',()=>{links.classList.toggle('open');document.body.classList.toggle('menu-open')});document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');document.body.classList.remove('menu-open')}));
const topButton=document.querySelector('.to-top');window.addEventListener('scroll',()=>topButton?.classList.toggle('show',scrollY>450));topButton?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(item=>item.classList.remove('active'));button.classList.add('active');const type=button.dataset.filter;document.querySelectorAll('.destination-card').forEach(card=>card.style.display=(type==='all'||card.dataset.type.includes(type))?'block':'none')}));
document.querySelectorAll('form').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();const toast=document.querySelector('.toast');toast.textContent='Rahmat! Xabaringiz qabul qilindi — tez orada bog‘lanamiz.';toast.classList.add('show');form.reset();setTimeout(()=>toast.classList.remove('show'),4200)}));
const slides=[...document.querySelectorAll('.hero-slide')],dots=[...document.querySelectorAll('.hero-dots button')];if(slides.length){let active=0;const show=index=>{slides[active].classList.remove('active');dots[active].classList.remove('active');active=index;slides[active].classList.add('active');dots[active].classList.add('active')};dots.forEach((dot,index)=>dot.addEventListener('click',()=>show(index)));setInterval(()=>show((active+1)%slides.length),5500)}
